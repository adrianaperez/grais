var errors = <%= @prototype.errors.count %>;

if(errors > 0){
  console.log("NO se actualizo el curso");
  
}else{

  $('#modal_edit_product_prototype').modal('close');
  $('.sidebar').empty();
  $('.sidebar').html("<%= escape_javascript (render partial: 'prototype_sidebar') %>"); 
}