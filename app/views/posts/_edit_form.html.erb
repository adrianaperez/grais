<%= form_for([post.course, post], remote: true) do |f| %>
  <div class="input-field ">
    <%= f.text_field :post_text %>
    <%= f.label :post_text, "Quieres comentar?" %>
  </div>
  <%= f.submit "Guardar", class: "btn" %>
<% end %>