<%= form_for([comment.post, comment], remote: true) do |f| %>
  <div class="input-field ">
    <%= f.text_field :comment_text %>
    <%= f.label :comment_text, "Quieres comentar?" %>
  </div>
  <%= f.submit "Comentar", class: "btn" %>
<% end %>