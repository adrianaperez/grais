var error= <%= @team.errors.count %>;

if(error>0){
  console.log("NO se actualizo el curso");
  
}else{

  $('#modal_edit_team').modal('close'); // Función definida en course.js
  $('.sidebar').empty();
  $('.sidebar').html("<%= escape_javascript (render partial: 'team_sidebar') %>"); 
}